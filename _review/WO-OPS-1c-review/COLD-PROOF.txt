ISOLATION — WARM + CLEAN-HOME COLD PROOF — WO-OPS-1c (at d3b670f)
================================================================
Per CTO: warm + cold, a CLEAN HOME (not just a clean store), the AUTH LINE
SHOWN, and the cold proof shows its own isolation (WO-7.1 evidence law).

WARM (working tree == committed bytes; git status clean at d3b670f):
  bash scripts/run-gates.sh → ALL GATES GREEN (41 tests; typecheck incl. the
  compile-guard type-test; zero-hardcode; copy-lint; lockfile URL-form;
  drift-check derived 0.9.8; Playwright 5/5; every negative fixture exit 1).

COLD (fresh $HOME, empty store, committed bytes of the pushed branch):

  $ export HOME=<fresh empty dir>          # its own isolation, shown:
      HOME=.../scratchpad/cold-1c/home
      ls -A "$HOME"                         → (empty — no inherited store/config)
      git config --global --list            → (none — clean)

  $ git config --global \
        url."http://<git-proxy>/git/".insteadOf "https://github.com/"
        # infrastructure, = CI's PLATFORM_CONTRACTS_READ_TOKEN step
  AUTH LINE SHOWN (verified set):
      url.http://<git-proxy>/git/.insteadof https://github.com/

  $ git clone --branch claude/platform-ops-console-90txec <origin> clone
      cold HEAD : d3b670fdd6982ecdc00130cf7822b04c3ea9ba64   (== origin)

  $ pnpm install --frozen-lockfile          # default store under fresh HOME = empty = cold
      Done — @platform/* resolved at pin 67bda02 (canon v0.9.8), lockfile frozen.

  GUARD: node -p "require('@platform/contracts/package.json').version" → 0.9.8

  cold PaymentOperatorActorSchema behavior (the delegated authority):
      ops:payment:sanou → true
      supplier:aicha    → false
      ops:payment:      → false   (empty suffix refused)

  Lockfile hygiene (cold): old-sha ba6f16d = 0 · new-sha 67bda02 = 28 ·
      git@github.com: = 0.

  $ bash scripts/run-gates.sh
      => ALL GATES GREEN (positives passed; every negative fixture exit 1 exactly)

No hand-edit of the lockfile: it was regenerated by `pnpm install` at the new
pin. The ssh-url negative fixture keeps the old sha on purpose — it tests URL
FORM, not pin currency.
